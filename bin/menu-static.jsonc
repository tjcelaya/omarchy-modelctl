{
  // modelctl — the quick menu only carries a launcher for the bar dropdown and the
  // agents submenu (searchable by name and path, summoned by the bar badge).
  "modelctl": {
    "icon": "󰚩",
    "label": "Local models",
    "aliases": ["models", "llm", "stable-diffusion"],
    "description": "Open the modelctl dropdown: LLMs, image models, agents",
    "action": "omarchy shell tjcelaya.modelctl toggle"
  },
  // @AGENT_ROWS@
}
